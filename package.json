{
  "name": "electron-auto-update-poc",
  "version": "1.0.1",
  "description": "A POC to figure out how to auto update electron apps installed.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node build.js",
    "electron": "electron app.js",
    "publish": "node build.js -p always"
  },
  "author": "sherin jacob",
  "license": "MIT",
  "dependencies": {
    "electron-log": "^4.3.5",
    "electron-updater": "^4.3.9"
  },
  "devDependencies": {
    "electron": "^8.2.2",
    "electron-builder": "^22.4.1"
  }
}
